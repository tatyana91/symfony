{% extends 'base.html.twig' %}

{% block title %}BookProduct{% endblock %}

{% block body %}
    <h1>BookProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book_product.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ book_product.title }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ book_product.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('book_product_index') }}">back to list</a>

    <a href="{{ path('book_product_edit', {'id': book_product.id}) }}">edit</a>

    {{ include('book_product/_delete_form.html.twig') }}
{% endblock %}
